-- migrations/0018_osm_poi_features_functions.up.sql
 --
 -- NOTE:
 -- This migration is intentionally a no-op.
 -- PostGIS is not available on some environments (e.g., Postgres installs without the postgis package
 -- or Postgres-compatible databases that don't support extensions / PL/pgSQL the same way).
 --
 -- Once PostGIS is installed/enabled and a local OSM import exists (planet_osm_* tables), create a
 -- follow-up migration to add POI cache tables and functions.
 
 SELECT 1;

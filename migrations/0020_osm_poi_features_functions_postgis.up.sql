-- This migration is intentionally empty to skip PostGIS-dependent features
-- The original migration required PostGIS extension which is not available in all databases
-- If PostGIS is needed, it should be installed separately and this migration can be re-enabled

SELECT 1; -- No-op migration to mark as completed
